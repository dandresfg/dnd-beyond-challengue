# Database Configuration
# Options: sqlite (default) or mysql
DB_TYPE=sqlite

# MySQL Configuration (only needed if DB_TYPE=mysql)
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASS=password
DB_NAME=dnd_challenge
